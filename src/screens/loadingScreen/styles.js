import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    text: {
        color: "white",
        padding: 30,
        fontWeight: "bold"
    },
    error: {
        color: "red",
        fontSize: 40,
        fontWeight: "bold"
    }
});